# 2026-07-20 工作日志

## 论文收尾阶段

### 图片美化
- 新建 `code/style_config.py` — 统一 4 个绘图脚本的 rcParams + seaborn white 底图 + 蓝#3B7DD8/红#D8413A/灰#6C7A89 统一配色
- 更新 `process.py` / `model.py` / `retention.py` / `shap_analysis.py` — 全部引用 style_config，配色统一
- 中文字体设置必须在 `sns.set_style` 之后，否则被覆盖
- 流程图中文字符（× ≤ → 、等）在 TikZ 中触发 fontspec 错误，需用数学模式替代

### 流程图（多轮迭代）
- 最终采用 matplotlib FancyBboxPatch 画流程图，保存为 `figures/flowchart.png`
- 2.问题分析.tex 引用 `flowchart.png`（替代 fbox 占位符和 TikZ 版本）
- 流程图节点命名对齐论文实际章节名（数据预处理→流失客户特征探索→数据准备→...）

### 符号表扩充
- 3.模型假设与符号说明.tex — 从 12 个扩展至 20 个，按五组分类（数据与特征/逻辑回归/评估与验证/可解释性与分层/策略评估）

### 参考文献升级
- ref.bib 从 6 条扩充至 16 条，新增 10 条 2022-2024 年文献
- 引用样式改为 unsrt
- 引言背景段增加 7 条引用形成文献综述式逻辑链
- 摘要关键词改为：客户流失预测；逻辑回归；SHAP 可解释性；客户分层；差异化挽留策略

### 附录
- 新增附录1（程序代码）— \lstinputlisting 引用 5 个 .py 文件
- 新增附录2（创新点说明）— 三模型实证选型/四层EDA/类别不平衡全流程/分位数分层
- 新增附录3（AI工具使用说明）— DeepSeek-V4-Flash+OpenCode 写代码，DeepSeek-V4-Pro 查知识，WorkBuddy 润色文字，含 4 条错误纠正
- 附录命名改为附录1/2/3（section 标题写死），不用 A/B/C
- 论文.tex 添加 \appendix + 三个 \input

### 文字质量诊断
- 核心分析段落已接近人类写作水平，问题三和模型评价章最优
- AI残留主要在过渡句和章首总起句，占全文 <5%
- 图片排版从 [H] 全局改为 [htbp]

### 编译状态
- xelatex 编译零错误，PDF 约 51 页
